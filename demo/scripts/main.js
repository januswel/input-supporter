(function () {
    'use strict';

    var app = angular.module('demo', ['input-supporters']);
    app.controller('demo-controller', [
        '$scope',
        function ($scope) {
        }
    ]);
})();
